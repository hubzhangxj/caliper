---
- name: del nginx on server
  shell: "rm -rf /tmp/nginx/"
  become: true
  become_method: sudo
  become_user: root

- name: stop nginx
  shell: "killall -9 nginx"
  become: true
  become_method: sudo 
  become_user: root
  ignore_error: yes
