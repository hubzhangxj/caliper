---
- name: install redis on server
  hosts: TestNode
  roles:
    - redis
  become: true
  become_method: sudo
  become_user: root

- name: start redis on server
  hosts: TestNode
  roles:
    - redis-server
  become: true
  become_method: sudo
  become_user: root

- name: install redis on client
  hosts: Device
  roles:
    - redis
  become: true
  become_method: sudo
  become_user: root

