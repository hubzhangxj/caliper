---
- name: Install redis on Client
  hosts: MasterDevice
  remote_user: root
  roles:
    - { role: ../../redis, redis_local_facts: no }

- name: Install redis on Server
  hosts: SlaveDevice
  remote_user: root
  roles:
    - { role: ../../redis, redis_local_facts: true }

